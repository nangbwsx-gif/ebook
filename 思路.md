# 电子样册系统 — 开发思路

## 项目概况

开发了一个多用户在线电子样册平台：用户上传 PDF 后，系统自动生成可在线翻页浏览的电子样册，每个用户拥有独立公开的书橱页面（如 `/bookcase/公司名`），客户无需登录即可翻阅。

项目地址：`https://github.com/你的用户名/你的仓库名`

---

## 分阶段开发记录

### 第一阶段：基础框架搭建

**目标**：搭建 Next.js 项目骨架，确定技术选型。

技术选型考虑：
- **Next.js 14**：全栈框架，一套代码搞定前端页面 + 后端 API，部署简单
- **SQLite + Prisma**：零配置数据库，不需要单独安装数据库服务，项目启动就能用
- **Tailwind CSS**：快速构建暗色主题 UI，不需要写手写 CSS
- **pdf.js**：浏览器端 PDF 渲染，不需要服务端 PDF 处理库

这一阶段创建了项目基础结构、数据库模型和核心组件骨架。

### 第二阶段：PDF 上传与阅读核心功能

**目标**：实现 PDF 上传 → 存储 → 在线翻页阅读的完整链路。

创建的接口：
- `POST /api/books` — 接收 PDF 文件，存储到本地文件系统，记录数据库
- `GET /api/books` — 获取书籍列表（支持按用户筛选）
- `GET /api/books/:id` — 获取单本书籍详情

创建的页面：
- **PDF 阅读器**（`src/components/PDFViewer.tsx`）：使用 pdf.js 将 PDF 渲染到 Canvas，支持翻页、缩放。这是核心组件
- **书架组件**（`src/components/BookShelf.tsx`）：网格布局展示书籍封面，按分类分组

### 第三阶段：PDF 阅读器翻页动画优化

**目标**：让 PDF 阅读体验更接近真实翻书，同时保持清晰度和性能。

实现方案：
- 使用两层 Canvas 叠加：上层显示当前页，下层预渲染目标页
- 翻页时通过 CSS `clip-path` 动画实现从右到左的擦除效果（模拟翻页）
- 底部添加装饰性的折角阴影和渐变，增加立体感
- 翻页完成后将下层内容复制到上层，避免重新渲染

关键逻辑：
- `skipNextRenderRef` 标记跳过翻页后的重复渲染，消除闪屏
- `turningRef` 防止动画播放期间重复触发
- `touchSwipedRef` 区分触摸滑动和轻触点击，防止移动端双击翻页

### 第四阶段：多用户系统

**目标**：从单用户变为多用户平台，每个用户独立管理自己的样册。

创建的功能：
- **注册**（`POST /api/auth/register`）：用户名 + 密码 + 自定义书橱标识（slug）
- **登录**（`POST /api/auth/login`）：验证密码，发放 Cookie Token
- **登出**（`POST /api/auth/logout`）：清除 Cookie
- **认证检测**（`GET /api/auth/check`）：验证当前登录状态

技术要点：
- 密码使用 bcryptjs 加密存储
- 登录状态通过 HttpOnly Cookie 传递（安全，防 XSS 窃取）
- Cookie 有效期 24 小时
- 所有 API 通过 `getCurrentUser()` 校验身份，数据按用户隔离

### 第五阶段：管理后台

**目标**：提供一个完整的管理界面，让用户管理自己的样册和分类。

创建的页面：
- **首页**（`/`）：产品落地页，展示功能介绍，引导注册
- **注册页**（`/register`）：用户注册表单
- **登录页**（`/admin/login`）：用户登录表单
- **管理后台**（`/dashboard`）：上传 PDF、管理书籍、查看书橱链接
- **书橱页**（`/bookcase/:slug`）：公开的书橱展示页，客户可浏览
- **阅读页**（`/book/:id`）：PDF 在线翻页阅读

管理后台功能：
- 上传 PDF 时可指定标题和分类
- 分类自动创建或复用已有分类
- 每一本书显示页数、分类、上传时间
- 可预览和删除样册

### 第六阶段：封面生成

**目标**：书橱中显示 PDF 第一页作为封面图，而非纯色占位。

实现方式（因为服务器不支持 PDF 渲染库）：
1. 用户打开书橱时，浏览器端的 pdf.js 读取 PDF 并渲染第 1 页到 Canvas
2. 渲染完成后，将 Canvas 导出为 JPEG 图片
3. 自动上传到服务器缓存（`PATCH /api/books/:id`）
4. 后续访问直接从缓存加载图片，无需重复渲染

这种方式的好处：不需要服务端安装 PDF 处理库，利用已加载的 pdf.js 即可完成。

### 第七阶段：Bug 修复与体验打磨

修复的问题：
1. **登录态丢失** — Cookie 缺少 `path: '/'` 导致页面跳转时丢失，在三个认证路由中修复
2. **翻页闪屏** — 翻页动画后 useEffect 重新渲染导致闪屏，用 `skipNextRenderRef` 跳过
3. **移动端双击翻页** — 触摸滑动和点击事件同时触发，用引用标记区分
4. **未分类书籍不显示** — 书橱只展示已分类书籍，添加"未分类"区域
5. **导航混乱** — 书橱右上角显示 `/admin` 链接不合理，改为根据登录态显示"进入管理"/"管理后台"
6. **返回箭头错误** — Dashboard 左上角箭头回到首页而非书橱页，修正为跳转用户书橱

### 第八阶段：部署准备

配置：
- `next.config.js`：`output: 'standalone'` 独立部署模式
- `Dockerfile` + `docker-compose.yml`：容器化部署方案
- `zeabur.yaml`：Zeabur 平台部署配置

---

## 技术架构总结

```
用户浏览器                        服务器 (Node.js)
┌─────────────────┐             ┌──────────────────────┐
│  首页 /          │             │  Next.js App Router   │
│  书橱 /bookcase  │──API请求──→│  ├── 页面 (SSR/SSG)    │
│  阅读器 /book    │             │  ├── API 路由         │
│  管理后台         │             │  │   ├── /api/auth/*  │
│  /dashboard      │←──响应────│  │   ├── /api/books/*  │
│                  │             │  │   └── /api/categories│
│  pdf.js (浏览器) │             │  ├── Prisma ORM       │
│  ← PDF 渲染      │             │  └── 文件系统 (PDF)   │
└─────────────────┘             └───────┬──────────────┘
                                        │
                                   ┌────▼──────┐
                                   │  SQLite   │
                                   │  (数据库) │
                                   └───────────┘
```

关键设计决策：
- **文件存储而非云存储**：避免依赖第三方云存储服务，减少费用和复杂度
- **客户端 PDF 渲染**：pdf.js 在浏览器中渲染，服务器只需存储和传输 PDF 文件
- **Cookie 认证而非 JWT/NextAuth**：简单直接，对于样册展示场景足够安全
- **SQLite 而非 MySQL**：零运维成本，单文件数据库，对于中小规模数据完全够用

---

## 部署方案

购买阿里云香港轻量服务器（¥24/月），部署流程：
1. 代码推送 GitHub
2. 服务器拉取代码，安装依赖
3. Node.js 直接运行生产构建
4. 绑定域名，配置 HTTPS 证书
5. 用 PM2 进程保活

--- 

*文档生成日期：2026-05-25*
